namespace SAPHelper
{
    public class ColunaDate : Coluna
    {
        public ColunaDate(string nome, string descricao, bool obrigatorio = false) : base(nome, descricao, obrigatorio)
        {

        }
    }
}
