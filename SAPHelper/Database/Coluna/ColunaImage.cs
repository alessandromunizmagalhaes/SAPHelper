namespace SAPHelper
{
    public class ColunaImage : Coluna
    {
        public ColunaImage(string nome, string descricao, bool obrigatorio = false) : base(nome, descricao, obrigatorio)
        {
        }
    }
}
